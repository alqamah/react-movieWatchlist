const initialState = {
  movies: []
};

const movieReducer = (state = initialState, action) => {
  switch (action.type) {
    // We'll add cases here later
    default:
      return state;
  }
};

export default movieReducer;